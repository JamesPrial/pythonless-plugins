# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This project is a [Claude Code Hook](https://code.claude.com/docs/en/hooks.md), written in Rust. ALWAYS PROACTIVELY use the Context7 MCP for information about the dependencies, beyond Rust stdlib. The dependencies and their library_ids are as follows:

serde = /websites/serde_rs
serde_json = /serde-rs/json
